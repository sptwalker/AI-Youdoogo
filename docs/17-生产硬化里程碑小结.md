# 17 · 生产硬化里程碑小结（H1–H3）

> 状态:阶段性小结。基于 docs/16 架构评审路线图，H1（生产硬化 P0）+ H2（可靠性 P1）
> + H3（质量闭环）三阶段十项已全部完成、真库验证、推送主线。H4（智能深化）待后续。

## 1. 完成范围

| 阶段 | 子项 | 提交 | 一句话成果 |
|---|---|---|---|
| **H1 生产硬化(P0)** | H1.1 密钥加密 | `62c3a92` | AI 卡片密钥 Fernet 透明加密,库里密文、读取明文 |
| | H1.2 行级隔离 | `9a60f2e` | 提案/任务/会议:普通员工只见本人+本部门,管理层全见 |
| | H1.3 注入防护 | `c155446` | spotlighting 包裹 KB 内容,挡 prompt injection |
| | H1.4 状态迁 Redis | `cf143f5` | 熔断+预算跨 worker 一致(Redis+本地兜底) |
| **H2 可靠性(P1)** | H2.1 调用护栏 | `325ddf8` | LLM 分级超时+failover 尝试上限+总超时封顶 |
| | H2.2 预算硬闸 | `2150b31` | 超预算硬拒绝+部门成本归因接线 |
| | H2.3 引用落库 | `0ad45e9` | AgentTaskRecord.sources 可重建"用了哪些资料";审计重试 |
| | H2.4 可观测 | `111cdc5` | Prometheus /metrics + /health/ready(真库验证) |
| **H3 质量闭环** | H3.1 评估驱动 | `e4feab2` | 评估集+LLM-as-Judge+影子对比,量化"改提示词是否变好" |
| | H3.2 结构化记忆 | `6d3d231` | 归档提炼成摘要/事实/实体/偏好,替代硬存 transcript |

## 2. 质量与验收

- **测试**:337 → **390**(+53),每阶段全量 `ruff + mypy + pytest` 全绿方进下一阶段。
- **迁移**:029 → **033**(密钥加密/task_record.sources/eval_case)。
- **真实验收**(非仅单测):
  - H1.1 解密密钥成功认证 provider(402 余额非密钥问题)
  - H1.4 跨进程实测:进程B 读到进程A 的熔断+预算原子累加(500+300=800)
  - H2.4 真库 curl `/metrics` 得真实用量(llm_calls success=75、tokens=252188)、`/health/ready` 回 200

## 3. 评审风险清单闭环情况（对照 docs/16 §2）

| 风险 | 级别 | 状态 |
|---|---|---|
| P0-1 API 密钥明文存库 | 🔴 | ✅ H1.1 Fernet 加密 |
| P0-2 业务表无行级隔离 | 🔴 | ✅ H1.2 row_filter/assert_can_see |
| P0-3 Prompt injection 敞口 | 🔴 | ✅ H1.3 spotlighting |
| P0-4 进程内状态多 worker 不一致 | 🔴 | ✅ H1.4 迁 Redis |
| P1-5 LLM 调用无护栏 | 🟠 | ✅ H2.1 超时+上限 |
| P1-6 预算软告警非硬闸 | 🟠 | ✅ H2.2 硬闸+归因 |
| P1-7 可观测性薄弱 + 引用不落库 | 🟠 | ✅ H2.3+H2.4 |
| P1-8 审计写失败静默 | 🟠 | ✅ H2.3 重试+ERROR |
| 评审最致命:自进化"无度量" | ⭐ | ✅ H3.1 评估驱动 |
| 长期记忆是弱 RAG | ⭐ | ✅ H3.2 结构化记忆 |

**P0/P1 全部闭环;评审判定 ROI 最高的两项(评估驱动 + 结构化记忆)已落地。**

## 4. 红线合规（贯穿未破）

十项改造无一新增 AI 生效权:密钥加密只改存储、行级隔离只收紧读、注入防护只护提示词、
评估只出分数、记忆只是资料。提示词是否应用、任务是否验收、决议是否生效——全部仍真人确认。

## 5. 待办：H4 智能深化（架构演进，未启）

- H4.1 Durable 编排(checkpoint + 可恢复);重估 LangGraph
- H4.2 共享黑板架构
- H4.3 反思/critic verify 回路

判断:H1–H3 已解决评审全部 P0/P1 与 ROI 最高项;H4 偏架构探索、锦上添花,可按需再做。
其中 H4.1 牵涉"是否重估暂缓 LangGraph 的决策",属较大架构选择,建议启动前先对齐。

## 6. 环境说明

- 生产镜像构建/完整栈起容器受本地环境外网限制(Docker Hub 不可达)未跑,留待有网环境;
  但首次部署最大风险(全套迁移空库跑通)已在真 Postgres 验证(docs/12 G.2b)。
- 开发库迁移已到 head(033);dev 分支为最新。
